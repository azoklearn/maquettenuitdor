# Base URL du site (pour les redirections Stripe)
BASE_URL=http://localhost:3000

# Port du serveur
PORT=3000

# Stripe (clé secrète, pas la clé publique)
# Créez un compte sur https://dashboard.stripe.com
STRIPE_SECRET_KEY=sk_test_xxxxx

# Secret du webhook Stripe (pour confirmer les réservations après paiement)
# À configurer dans Stripe > Développeurs > Webhooks > Ajouter un endpoint
# URL : https://votre-domaine.com/api/webhook/stripe
# Événement : checkout.session.completed
STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# Prix de base par nuit (euros)
BASE_PRICE_PER_NIGHT=150

# Email de confirmation (Resend)
# Créez un compte sur https://resend.com et récupérez une clé API
# RESEND_API_KEY=re_xxxxx
# Adresse d'envoi (avec domaine vérifié sur Resend, ou onboarding@resend.dev pour test)
# EMAIL_FROM=Nuit d'Or <contact@votredomaine.com>
# Copie de chaque confirmation envoyée à cette adresse (optionnel)
# NOTIFY_EMAIL=votre@email.com

# Chemin optionnel vers la base SQLite (par défaut : data/nuitdor.db)
# SQLITE_PATH=./data/nuitdor.db

# Codes promo : format "CODE1:10,CODE2:15" (pourcentage de remise, 1-99)
# Ex. ROMANCE:10 = 10 % de réduction, BIENVENUE:15 = 15 %
# PROMO_CODES=ROMANCE:10,BIENVENUE:15
